text = fileread('rfc2795.txt');

% nr 1
[y, x] = groupcounts(double(text)') ;

figure;

plot(x,y,'Color',[0,1.0,0]);

xlabel('Zeichen (sortiert)');
ylabel('Häufigkeit');


% nr 2
[v, i] = maxk(y, 10);

bar(v); set(h,'FaceColor','k');
set(gca,'xticklabel',char(x(i)));
xlabel('Zeichen');
ylabel('Häufigkeit');

% t = 'A' : 'Z';
% text = upper(text);

% hist(double(text),1:90);
% n = hist(double(text),1:90); 
% plot(double(t), n(t));
% n(t)